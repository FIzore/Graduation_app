# QLUT 二手平台前端进度审计报告

## 一、状态审计：各页面/模块成熟度评估

基于 `前端详细流程.md` 四大阶段，逐项核对实际代码实现情况：

### Phase 0 - 基础设施 ✅ 100%
- UniApp (Vue3 + TypeScript) 项目初始化完成
- 已集成 `@dcloudio/uni-ui` 组件库
- 微信小程序 AppID 已配置 (`manifest.json:34`)

### Phase 1 - 网络基建层 ✅ 100%
- `utils/request.ts`：Token 自动注入、401 鉴权拦截自动跳登录
- BaseURL 配置为 `http://127.0.0.1:8080/api/v1`
- 统一响应模型封装 (`HttpResponse<T>`)

### Phase 2 - 核心页面静态切图 ✅ 95%
| 页面/组件 | 路径 | 状态 | 评级 |
|-----------|------|------|------|
| TabBar 导航 | `pages.json` + `custom-tabbar.vue` | 完成：首页/发布/消息/我的 | ⭐生产级 |
| 首页瀑布流 | `index.vue` | 完成：双列布局、下拉刷新、上拉分页、图片解析、Mock 回退 | ⭐生产级 |
| 物品详情页 | `detail.vue` | 完成：轮播图、状态标签、预约按钮、图片预览 | ⭐生产级 |
| 发布页 | `post.vue` | 完成：多图选择、逐张上传、表单校验、FAB 发布按钮 | ⭐生产级 |
| 消息页 | `message.vue` | **仅占位**：`<text>消息功能开发中...</text>` | ❌未开始 |

### Phase 3 - 前后端大动脉打通 ✅ 90%
| 功能 | 状态 | 说明 |
|------|------|------|
| 登录/注册 | ✅ | `login.vue` 完整实现，Token 持久化，微信绑定预留 |
| 图片上传 | ✅ | `uploadImage()` 封装 `uni.uploadFile`，`post.vue` 中调用 |
| 物品发布 | ✅ | `createItem()` 调用，发布后 `reLaunch` 回首页 |
| 物品列表/详情 | ✅ | `getItems()` + `getItemDetail()` 已对接 |
| 预约交接 | ⚠️ | `createAppointment()` 已对接，但无"确认交接完成"动作和 UI |

### Phase 4 - 高级业务 & WebSocket ❌ 10%
| 功能 | 状态 | 说明 |
|------|------|------|
| WebSocket 私聊 | ❌ | `utils/websocket.ts` 未创建，`pages/chat/room.vue` 不存在 |
| 消息页面 | ❌ | `message.vue` 仅有一行占位文字 |
| 状态流转闭环 | ⚠️ | 预约创建已实现，但缺少卖家侧"确认交接完成"按钮 |
| 搜索功能 | ❌ | 首页搜索栏仅为静态装饰 `<text>`，无实际搜索交互 |

### 缺失的子页面
| 路径 | 引用位置 | 当前状态 |
|------|----------|----------|
| `/pages/settings/settings` | `profile.vue:51` | **不存在** |
| `/pages/chat/room` | TECH_DESIGN 提及 | **不存在** |
| 想要的(收藏) | `profile.vue` 统计栏 | **无页面** |

---

## 二、逻辑对齐：字段兼容性与 ID 匹配审计

### 2.1 camelCase / PascalCase 双重兼容：良好
所有页面模板均广泛使用 `||` 短路链做字段回退：

```
index.vue:21    :key="item.id || (item as any).ID"
index.vue:29    {{ item.title || (item as any).Title || '未命名物品' }}
detail.vue:88   item.value?.ID || item.value?.id
appointments.vue:20  app.AppointmentTime || app.appointment_time || app.CreatedAt || app.created_at
```

**结论**：运行时兼容性覆盖全面，渲染失败风险低。

### 2.2 ID 匹配 String() 转换：基本到位但存在薄弱点
- `profile.vue:211` & `published.vue:161` 均使用 `String(item.PublisherID || item.publisher_id) === currentUserId` ✅
- `profile.vue:172` 使用 `String(parsedUserInfo.ID || parsedUserInfo.id || '')` ✅
- `getUserIdFromToken()` 返回 `String(data.user_id || data.id || ...)` ✅
- `index.vue:22`：`goToDetail(item.id || (item as any).ID)` — 未做 `String()`，但作为路由参数 Uni 框架会自动序列化，`detail.vue:102` 用 `Number(options.id)` 反序列化。**无风险。**
- `detail.vue:139`：`createAppointment(itemId.value)` — `itemId` 是 `computed` 返回 `.ID || .id`，可能为 `0`（fallback）。若后端 `item_id` 为 0 会报参数错误。**低风险**。

### 2.3 乱码相关：登录页 `login.vue:76` 导入 `login` 函数时：
```
import { login, register, bindWeChat } from '../../api/user';
```
API 层 `user.ts:7` 有 `console.log('Sending Auth Params (Login):', data)` — 仅限调试用，无乱码风险。

### 2.4 防红线词汇审查：合规
代码全文检索未发现 `buy`, `pay`, `order`, `cart`, `checkout` 等违禁词。
✅ 使用 `reserve`(预约), `handover`(交接), `appointment`(预约), `pending`(锁定中)

---

## 三、与 API 技术规范冲突项

| 规范要求 | 实际实现 | 判定 |
|---------|---------|------|
| Base URL `http://localhost:8080/api/v1` | `127.0.0.1:8080/api/v1` | ⚠️ 域名不一致，但均指向本机 |
| POST /appointments 参数 `item_id: number` | `createAppointment(itemId.value)` — `item.ts:42` 正确 | ✅ |
| PUT /appointments/:id/confirm | **前端未调用** | ❌缺失 |
| /user/appointments 返回嵌套 item 实体 | `appointments.vue:92` 做了 `app?.item \|\| app?.Item` 兼容 | ✅ |
| WS 连接 URL `ws://localhost:8080/api/v1/ws?token=<JWT>` | 未实现 | ❌缺失 |
| Item.images 后端存 JSON 字符串，前端需兼容解析 | `parseImages()` 函数在所有页面均有实现 | ✅ |

**图片 BaseURL 不一致**：`index.vue:155` 硬编码 `http://127.0.0.1:8080`，`appointments.vue:50` 也硬编码，`detail.vue:84` 同样。`post.vue` 则使用 `serverUrl` 变量。缺乏统一的图片 BaseURL 常量。

---

## 四、最优先三项开发任务（按业务闭环优先级排序）

### 任务 1：WebSocket 私聊模块（对齐 Phase 4）
**影响范围**：`message.vue`(消息列表) + `utils/websocket.ts` + `pages/chat/room.vue`(聊天室)
- 创建 `utils/websocket.ts`：封装 `uni.connectSocket`，实现心跳保活、断线重连、消息分发
- 重构 `message.vue`：从占位页面改为会话列表（展示最近联系人和未读数）
- 新建 `pages/chat/room.vue`：气泡聊天界面，支持文字消息发送
- API 需补充：`/chat/history`（获取历史消息）、`/chat/conversations`（会话列表）
- **理由**：消息是 TabBar 四大入口之一，当前仅占位，是用户感知度最高的缺失功能。

### 任务 2：预约状态流转闭环（对齐 Phase 4 + API 规范）
**影响范围**：`detail.vue` + `appointments.vue`
- 在 `item.ts` 补充 `confirmAppointment` 接口（PUT /appointments/:id/confirm）
- 在 `detail.vue` 底部栏增加卖家视角判断：若当前用户 == PublisherID 且 status == 'Pending'，显示"确认交接完成"按钮
- 在 `appointments.vue` 中增加卖家侧"确认完成"操作入口
- **理由**：预约创建已实现，但缺少确认完成动作导致状态机无法闭环，这是 PRD 3.3 节的核心业务流程。

### 任务 3：搜索与空状态完善（提升 MVP 体验）
**影响范围**：`index.vue` + 新建 `pages/settings/settings.vue`
- 首页搜索栏接入 `GET /items` 的 `keyword` 参数（若后端支持）或前端本地过滤
- 创建 `pages/settings/settings.vue` 基础设置页（`profile.vue` 已引用该路由但页面不存在）
- 统一图片 BaseURL 常量化（消除各页面中重复的硬编码 `http://127.0.0.1:8080`）
- **理由**：搜索是信息撮合平台的核心交互，当前完全不可用；settings 页面缺失会导致运行时路由错误。

---

## 五、技术债务清单（非阻塞但建议关注）

1. **图片 URL 拼接硬编码**：4 个页面各自硬编码 `http://127.0.0.1:8080`，应抽取到 `utils/request.ts` 导出常量。✅ **已解决 (v0.7)** — `config/index.ts` 统一导出 `IMAGE_BASE_URL`。
2. **TypeScript 类型不匹配**：`api/item.ts` 定义 `Item` 接口字段为 camelCase，但后端返回 PascalCase。建议定义联合类型或运行时适配层。
3. **"我发布的"数据获取低效**：`profile.vue` 和 `published.vue` 通过 `getItems({pageSize: 500})` 全量拉取后前端过滤，无后端 dedicated API。
4. **静态资源缺失**：代码中引用了 `/static/empty.png` 和 `/static/default.png`，但 `static/` 目录下仅有 `default-avatar.png`。
5. **"想要的"统计始终硬编码为 0**：`profile.vue:23` 写死 `<text class="count">0</text>`，未对接后端。
6. **详情页无"私聊"按钮**：TECH_DESIGN 3.1 要求详情页底部有"私聊"和"发起交接"两个按钮，当前仅有预约按钮。

---

## 六、WebSocket 实时通讯模块 — 超小步迭代计划 (Phase 4)

### 后端对齐基准

| 后端版本 | 功能 | 前端对接要求 |
|---------|------|------------|
| v0.17 | WS 文字聊天 + 历史查询 `GET /api/v1/chat/history` | 消息收发 + 历史记录拉取 |
| v0.22 | 60s 读超时 / 30s Ping / 10s 写超时 | 前端响应 Ping、主动 Pong |
| v0.23 | 统一 camelCase JSON 标签 (`toId`, `content`, `isRead`) | 前端消息格式必须 camelCase |

**WebSocket 连接 URL**：`ws://127.0.0.1:8080/api/v1/ws?token=<JWT>`

---

### v0.8.1：底层通信引擎基建

**目标**：创建 `utils/websocket.ts`，提供可靠的 WebSocket 连接管理能力。不涉及任何 UI。

**创建 2 个文件**：

#### 1.1 `config/index.ts` — 追加 WS_URL 常量

```typescript
export const WS_URL = `ws://127.0.0.1:8080/api/v1/ws`;
```

#### 1.2 `utils/websocket.ts` — WebSocket 引擎（单例 + 心跳 + 重连 + Pub/Sub）

| 模块 | 实现要点 |
|------|---------|
| **单例模式** | 导出 `useWebSocket()` 函数，闭包内维护唯一 `SocketTask` 实例，多次调用返回同一实例 |
| **Token 鉴权** | 从 `uni.getStorageSync('token')` 读取 JWT，拼接 `?token=xxx` 作为连接 URL |
| **指数退避重连** | 断开后延迟重连，间隔序列：1s → 2s → 4s → 8s → 16s → 32s (上限)。连接成功或手动关闭时重置计数器 |
| **30s Ping 心跳** | 后端每 30s 发送 Ping 帧，`uni.connectSocket` 底层自动响应 Pong。另起一个 45s 定时器：若连续 45s 未收到任何消息（含 Ping），主动断开重连 |
| **Pub/Sub 事件系统** | 维护 `listeners: Map<string, Set<Function>>`，对外暴露 `on(event, callback)` 和 `off(event, callback)` 接口。收到服务端消息时触发 `'message'` 事件；连接状态变更时触发 `'open'` / `'close'` / `'error'` 事件 |
| **发送方法** | `send(data: object)` 包装 `socket.send({ data: JSON.stringify(data) })`，连接未就绪时排队到 `pendingQueue` |
| **生命周期** | `connect()` 建立连接并启动心跳；`disconnect()` 停止重连、关闭 socket、清空回调 |

**验收标准**：App.vue `onLaunch` 中调用 `connect()`，控制台可见 `[WS] connected` / `[WS] message received` 日志。

---

### v0.8.2：全局状态与会话列表壳子

**目标**：状态共享层 + `pages/message/message.vue` 重构为最近联系人列表。

**依赖**：v0.8.1 完成

**创建/修改 3 个文件**：

#### 2.1 `store/conversation.ts` — 全局会话状态（Vue3 reactive 单例）

由于项目未引入 Pinia，使用 `reactive()` 导出的模块级单例实现跨组件状态共享：

| 状态字段 | 类型 | 说明 |
|---------|------|------|
| `conversations` | `Map<number, Conversation>` | 以对方 userId 为 key 的会话映射 |
| `unreadTotal` | `computed` | 所有会话未读数求和 |
| `updateConversation(msg)` | 方法 | 收到 WS 消息时更新/创建会话，递增未读数 |

`Conversation` 接口：`{ userId, nickname, avatar, lastMsg, lastTime, unreadCount }`

#### 2.2 `App.vue` — WS 连接生命周期 + 消息拦截

修改 `onLaunch`：
1. 调用 `ws.connect()` 启动 WebSocket
2. 注册 `ws.on('message', handler)` 监听所有服务端消息
3. 在 handler 中调用 `conversationStore.updateConversation(msg)` 更新会话状态
4. `onHide` 时可选关闭连接（视策略而定，建议保持连接）

#### 2.3 `pages/message/message.vue` — 会话列表重构

从占位页面改为静态 UI 列表：
- 读取 `conversationStore.conversations` 渲染会话卡片（头像 + 昵称 + 最后一条消息 + 时间戳 + 未读红点）
- 点击卡片 `navigateTo` → `pages/chat/room?userId=X&nickname=Y`（接收方 userId 和昵称通过 URL 参数传递）
- 空状态：`<uni-icons type="chat" size="80" color="#ccc" />` + "暂无消息"
- 接入全局未读数，将 `unreadTotal` 透传给 `custom-tabbar.vue` 显示红点

**验收标准**：启动应用后底部"消息"Tab 可看到列表页面框架（即使暂无会话数据，显示空状态亦可）。

---

### v0.8.3：聊天室核心 UI 与实时收发

**目标**：创建 `pages/chat/room.vue`，实现双向实时消息。

**依赖**：v0.8.1 + v0.8.2 完成

**创建 1 个文件 + 修改 2 个文件**：

#### 3.1 `pages/chat/room.vue` — 聊天室页面

| 区域 | 实现细节 |
|------|---------|
| **顶部导航栏** | 自定义导航：返回箭头 + 对方昵称（从 `onLoad(options)` URL 参数 `nickname` 获取） |
| **消息列表** | `<scroll-view>` 纵向滚动，自动滚底。每条消息气泡：左侧白色（对方，带小箭头）、右侧绿色（自己 `#95ec69`，仿微信风格）。显示消息文本和时间戳 |
| **输入栏** | 底部固定在 `safe-area` 上的 `<input>` + 发送按钮，`@keyboardheightchange` 自动顶起消息列表 |
| **发送逻辑** | 组装 `{ toId: number, content: string }`，调用 `ws.send(data)`。消息同时追加到本地 `messages[]` 数组中显示（乐观更新） |
| **接收逻辑** | `onLoad` 中注册 `ws.on('message', handler)`，过滤 `fromId === userId` 的消息追加到本地列表并自动滚动 |
| **消息格式兼容** | 后端消息字段使用 camelCase (`fromId`, `toId`, `content`, `createdAt`)，渲染时同时兼容 PascalCase 回退 (`FromId` 等) |

#### 3.2 `pages.json` — 注册新路由

```json
{ "path": "pages/chat/room", "style": { "navigationStyle": "custom" } }
```

#### 3.3 `components/custom-tabbar.vue` — 消息红点

`active === 'message'` 时，在消息图标右上角显示 `.badge` 红色圆点，通过 prop 或 store 获取 `unreadTotal > 0` 状态。

**验收标准**：从消息列表点击某会话进入聊天室，发送文字消息后对方（可用两个模拟器测试）立即收到并在屏幕上显示。

---

### v0.8.4：历史消息缝合与细节打磨

**目标**：进入聊天室先加载历史记录，再衔接实时消息。补齐加载态与空态。

**依赖**：v0.8.3 完成

**创建/修改 3 个文件**：

#### 4.1 `api/chat.ts` — 聊天 HTTP API 封装

```typescript
export const getChatHistory = (otherId: number, page: number, pageSize: number) =>
  request<{ messages: Message[]; total: number }>('/chat/history', 'GET', {
    other_id: otherId, page, page_size: pageSize
  });

// Message 接口
export interface Message {
  fromId: number;
  toId: number;
  content: string;
  isRead: boolean;
  createdAt: string;
}
```

#### 4.2 `pages/chat/room.vue` — 融入历史消息

修改 `onLoad` 逻辑：
1. 先调用 `getChatHistory(userId, 1, 50)` 拉取最近 50 条消息
2. 渲染历史消息到 `messages[]` 数组顶部（倒序插入，最早在上、最新在下）
3. 渲染完毕后注册 WS 消息监听，此后实时消息追加到数组尾部
4. 加载历史消息时显示 `uni.showLoading`，完成后 `hideLoading`
5. 触顶下拉加载更早历史（分页参数 `page += 1`）

#### 4.3 `pages/message/message.vue` — 会话列表接入真实数据

将 v0.8.2 的静态列表升级：
- `onShow` 时从 `conversationStore` 读取会话列表（已是真实数据）
- 按 `lastTime` 倒序排列
- 用 `formatTime()` 工具函数统一时间显示（今天显示 HH:mm，昨天显示"昨天"，更早显示 MM-DD）

**验收标准**：进入聊天室先看到加载 Loading，然后渲染历史消息，再收到新消息时无缝拼接在底部。下拉可加载更早记录。

---

### 交互规则

1. 每个小版本完成后**必须停手**，等待 Debug 和 Git Commit。
2. 未经明确指令，绝不跨版本编写代码。
3. 代码严格遵守 `AGENTS.md` 红线：camelCase 消息格式、无支付/购买词汇、Vue3 setup 语法糖。

---

> Plan 已就绪。请切换到 Code/Act 模式并回复 `开始 v0.8.1`，我将开始第一阶段的编码。
